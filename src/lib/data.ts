// This file is deprecated and its contents have been moved to more specific files
// like `lib/constants.ts` for static data and `lib/api.ts` for data fetching.
// It is kept here to prevent build errors from old imports but can be safely removed
// once all components are updated.

export {};
