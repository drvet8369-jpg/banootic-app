# ------------------------------------------------------------------------------
# Supabase General Keys (Required for the App to work)
# Find these in your Supabase project dashboard under: Project Settings > API
# ------------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL="{YOUR_PROJECT_URL}"
NEXT_PUBLIC_SUPABASE_ANON_KEY="{YOUR_ANON_PUBLIC_KEY}"


# ------------------------------------------------------------------------------
# Supabase Admin Keys (Required for server-side operations & scripts)
# Find this in your Supabase project dashboard under: Project Settings > API
# It is located in the "Project API keys" section. Use the 'service_role' secret.
# ------------------------------------------------------------------------------
SUPABASE_SERVICE_ROLE_KEY="{YOUR_SERVICE_ROLE_KEY}"


# ------------------------------------------------------------------------------
# Supabase Direct Database Connection (Required ONLY for the `npm run db:push` command)
# Find this in your Supabase project dashboard under: Project Settings > Database > Connection string (URI)
# IMPORTANT: Copy the full URI and only replace [YOUR-PASSWORD] with your actual database password.
# ------------------------------------------------------------------------------
SUPABASE_DB_CONNECTION="postgresql://postgres:[YOUR-PASSWORD]@db.{YOUR_PROJECT_ID}.supabase.co:5432/postgres"


# ------------------------------------------------------------------------------
# Kavenegar SMS Provider Keys (Required for OTP SMS messages to work)
# Find these in your Kavenegar panel. You will need to set them as secrets in Supabase.
# See Supabase Docs: Functions > Edge Functions > Secrets
# ------------------------------------------------------------------------------
# KAVEHNEGAR_API_KEY="{YOUR_KAVENEGAR_API_KEY}"
# KAVEHNEGAR_SENDER_NUMBER="{YOUR_KAVENEGAR_SENDER_NUMBER}"
