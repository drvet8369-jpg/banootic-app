# Environment variables for Supabase.
# IMPORTANT: This file is a template. Copy its contents to a new file named .env in the project root
# and fill in your actual Supabase project details.

# 1. Supabase Project URL
# Find this in your Supabase project dashboard under Project Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL="{آدرس_URL_پروژه_شما_در_اینجا}"

# 2. Supabase Anon Key (Public Key)
# Find this in your Supabase project dashboard under Project Settings > API > Project API Keys > anon (public)
NEXT_PUBLIC_SUPABASE_ANON_KEY="{کلید_ANON_پروژه_شما_در_اینجا}"

# 3. Supabase Service Role Key (Secret Key)
# Find this in your Supabase project dashboard under Project Settings > API > Project API Keys > service_role (secret)
# IMPORTANT: This key must be kept secret and should never be exposed on the client-side.
SUPABASE_SERVICE_ROLE_KEY="{کلید_SERVICE_ROLE_پروژه_شما_در_اینجا}"

# 4. Kavenegar API Key (Secret)
# Get this from your Kavenegar account dashboard.
KAVEHNEGAR_API_KEY="{کلید_API_کاوه‌نگار_شما_در_اینجا}"

# 5. Kavenegar Sender Number (Secret)
# This is your dedicated sender number from Kavenegar.
KAVEHNEGAR_SENDER_NUMBER="{شماره_فرستنده_کاوه‌نگار_شما_در_اینجا}"

# 6. Database Connection String (URI)
# Find this in your Supabase project dashboard under Project Settings > Database > Connection string > URI.
# ---
# VERY IMPORTANT INSTRUCTIONS:
# a) Copy the FULL URI string from the Supabase dashboard.
# b) Paste it here, inside the double quotes.
# c) Find the placeholder [YOUR-PASSWORD] inside the string you just pasted.
# d) Replace ONLY the text "[YOUR-PASSWORD]" with your actual database password. DO NOT remove or add any other characters.
#    Correct: postgres://postgres:MyActualPassword@...
#    INCORRECT: postgres://postgres:[MyActualPassword]@...  <-- DO NOT keep the brackets [].
# ---
SUPABASE_DB_CONNECTION="{آدرس_کامل_URI_که_از_سایت_کپی_کردید_را_اینجا_قرار_دهید}"
